// in properties/mysql-build-properties.xml values DB.URL/DB.USER/DB.PASSWORD can be used to hardcode db values
// jdbc:sqlserver://;servername=server_name;encrypt=true;integratedSecurity=true;authenticationScheme=JavaKerberos
//  - can also be used to set while running? mainly just placeholders

import java.sql.*; //Java sql tools and JDBC
public class user {
    
}
